{
  "name": "Event Validation Streaming Pipeline",
  "description": "Validates Pub/Sub JSON events and routes valid and invalid records to GCS.",
  "parameters": [
    {
      "name": "input_subscription",
      "label": "Input Pub/Sub subscription",
      "helpText": "projects/PROJECT/subscriptions/SUBSCRIPTION",
      "regexes": ["^projects/[^/]+/subscriptions/[^/]+$"]
    },
    {
      "name": "valid_output",
      "label": "Valid output prefix",
      "helpText": "GCS prefix for valid JSONL records",
      "regexes": ["^gs://.*$"]
    },
    {
      "name": "invalid_output",
      "label": "Invalid output prefix",
      "helpText": "GCS prefix for invalid JSONL records",
      "regexes": ["^gs://.*$"]
    }
  ]
}

